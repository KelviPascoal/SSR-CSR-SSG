import style from "@emotion/styled";

export const Wrapper = style.div`
    padding: 1rem;
    background-color: #9340bd;
    margin: 0.4rem;
    color: #ffffff;
    width: 8rem;
    border-radius: 0.8rem;

    display: flex;
    gap: 0.4rem;
    flex-direction: column;
    justify-content: center;
    align-items: center;
`;
